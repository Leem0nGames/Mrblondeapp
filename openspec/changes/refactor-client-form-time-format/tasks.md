## 1. Helper Function
- [ ] 1.1 Create a helper function `generateTimeOptions` that returns an array of strings representing times in 24-hour format (e.g., `["08:00", "08:30", ..., "20:00"]`). This can be placed inside the form components.

## 2. Update Admin Client Form
- [ ] 2.1 In `src/app/admin/clients/_components/upsert-client-form.tsx`:
- [ ] 2.2 Replace the `<Input type="time">` for `delivery_time_from` and `delivery_time_to` with `<Select>` components.
- [ ] 2.3 Populate the `<Select>` components with the options generated by the `generateTimeOptions` helper function.

## 3. Update Onboarding Client Form
- [ ] 3.1 In `src/app/onboarding/[token]/_components/onboarding-form.tsx`:
- [ ] 3.2 Apply the same changes as in the admin form, replacing the time inputs with 24-hour format `<Select>` components.

## 4. Final Review
- [ ] 4.1 Verify that the time selectors appear correctly in both the admin "create/edit client" dialog and the public onboarding page.
- [ ] 4.2 Confirm that saving the form correctly stores the selected 24-hour format time.
- [ ] 4.3 Mark this task list as complete.
